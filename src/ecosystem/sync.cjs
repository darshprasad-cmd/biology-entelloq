const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'../..'),block=fs.readFileSync(path.join(__dirname,'../_switcher.html'),'utf8');for(const f of fs.readdirSync(root).filter(f=>f.endsWith('.html')&&f!=='404.html')){const p=path.join(root,f);let s=fs.readFileSync(p,'utf8');const re=/<!-- ENTELLOQ-ECOSYSTEM-SWITCHER:START -->[\s\S]*?<!-- ENTELLOQ-ECOSYSTEM-SWITCHER:END -->/;s=re.test(s)?s.replace(re,block):s.replace('</body>',block+'\n</body>');fs.writeFileSync(p,s);}